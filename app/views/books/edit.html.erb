<h1>Editing Book</h1>

 <% if @book.errors.any? %>
   <p>"can't be blank"</p>
<% end %>


 <%= render 'books/form', book: @book %>


<%= link_to 'Show', @book, class: "show_#{@book.id}" %> |
<%= link_to 'Back', books_path, class: "back" %>
